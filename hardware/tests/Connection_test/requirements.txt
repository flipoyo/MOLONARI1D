Tu clones le github (https://github.com/nanopb/nanopb) la, tu installes nanopb via pixi (`pixi add nanopb`) puis normalement avec $  python .\nanopb\generator\nanopb_generator.py sensor.proto ça devrait marcher
Attention a se placer dans le chemin ou y a sensor.proto et bien specifier le path vers le ficher python
